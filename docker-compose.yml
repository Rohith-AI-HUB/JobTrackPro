version: '3.8'

services:
  # JobTracker App
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: jobtracker-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      MONGODB_URL: mongodb+srv://rohithb892:8nxeAeM4B0r20Bua@jobtracker.0qgrtrx.mongodb.net/?retryWrites=true&w=majority&appName=JobTracker
      PORT: 5000
    networks:
      - jobtracker-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/dashboard/stats"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  jobtracker-network:
    driver: bridge